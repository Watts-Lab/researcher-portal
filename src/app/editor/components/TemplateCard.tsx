// when the timeline renderer encounters a template Context, (https://deliberation-lab.readthedocs.io/en/latest/templates/#template-context)
// then it calls this card with
//  - the entire template context
//  - the list of templates that can be populated

// on first render, just display the information from the template context

// has three modes:
// - expand template
// - template context
// - raw template itself

// only render a template in the render panel when its expanded

//export function TemplateCard({ templateObject, templatesList })
