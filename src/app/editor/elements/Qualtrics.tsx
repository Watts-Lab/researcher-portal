export default function Qualtrics({ url, params, onSubmit }) {
  return (
    <div>
      <h1>Qualtrics</h1>
      <p> `url: ${url}` </p>
      <p> `params: ${params}` </p>
      <p> `onSubmit: ${onSubmit}` </p>
    </div>
  );
}
